/** * A class that represents a comparable object that can be in a priority queue. *  * @author Frank M. Carrano * @version 3.0 */public class Thing<T> implements Comparable<Thing<T>> {	private T data;	private int priority;	public Thing(T dataValue, int priorityValue) {		data = dataValue;		priority = priorityValue;	} // end constructor	public T getData() {		return data;	} // end getData	public int getPriority() {		return priority;	} // end getPriority	public void setData(T newData) {		data = newData;	} // end setData	public void setPriority(int newPriority) {		priority = newPriority;	} // end setPriority	@Override	public int compareTo(Thing<T> other) {		int result;		if (priority == other.priority)			result = 0;		else if (priority < other.priority)			result = -1;		else			result = 1;		return result;	} // end compareTo	@Override	public String toString() {		return data.toString() + " " + priority;	} // end toString} // end Thing